{
  "name": "recap-express",
  "version": "1.0.0",
  "description": "For this recap we will model a simple express REST API server and create CRUD operations for a resource called 'terms', which will represent glossary terms with a definition.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
